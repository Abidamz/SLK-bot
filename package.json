{
  "name": "slk-bot",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "npm --prefix worker test --",
    "typecheck": "npm --prefix worker run typecheck",
    "deploy:worker": "npm --prefix worker run deploy",
    "deploy:pages": "npx wrangler pages deploy dashboard --project-name=slk-radar --branch=main"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
